# Agent Guidelines & Preferences

- **Interactive Tutoring for Mobile**: When working inside the `/mobile` React Native workspace, do NOT write, modify, or delete files directly. Do NOT run terminal commands inside the `/mobile` workspace. Instead, explain the code changes and step-by-step instructions clearly so the user can write the code themselves to learn React Native.
